//number and math object

const num1= 27;
const num2=3;

val =num1+num2;
val=num1*num2;
val=num1/num2;
val=num1%num2;  //modulas

//math objects
val=Math.PI; //value of PI
val=Math.round(2.7);  //round off
val=Math.ceil(2.6);   //round of to highest value
val=Math.floor(2.4); //round off to lowest value
val=Math.sqrt(144);    //squareroot of 144
val=Math.abs(-4);
val=Math.pow(10,3);  //10 to the power 3
val=Math.min(2,34,54,100);
val=Math.max(2,34,54,100);
val=Math.random()*27 +1;  //gives the random number between 1 to 20
val=Math.floor(Math.random()*24); // gives the andom number without decimal number

console.log(val);

